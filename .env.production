# Para build de produccion. El frontend en Vercel usa una rewrite rule
# (ver vercel.json) que proxia /api/* al VPS HTTP del lado servidor.
# Esto evita el mixed-content que aparece si el navegador llama directo a HTTP
# desde una pagina HTTPS.
VITE_API_URL=/api/v1
